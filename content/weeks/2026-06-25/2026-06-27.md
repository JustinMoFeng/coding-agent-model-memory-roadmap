# 2026-06-27

## 今日目标

- A 线：理解 causal LM loss、SFT dataset format 和 generation 参数的关系。
- B 线：阅读 Multica prompt construction 与 task lifecycle。
- 产出：一个 coding-agent trajectory 改写成 SFT 样本的例子，以及一个 task lifecycle 图。

## 学习设计

### A 线：Causal LM、SFT Format、Generation

材料：

- Hugging Face Transformers causal language modeling 文档。
- TRL SFTTrainer / dataset formats 文档。
- 只补充理解 greedy decoding、temperature、top-p 的基本区别。

要搞清楚：

- `input_ids`、`labels`、`attention_mask` 在 causal LM 里分别是什么。
- prompt-completion dataset 和 conversational dataset 的区别。
- 为什么 SFT 的本质是让模型在给定上下文下模仿目标输出。
- 为什么 rollout/eval 时 generation 参数必须记录清楚。

建议动作：

- 写一个最小例子：输入文本、token、label、被 mask 的位置、loss 计算对象。
- 把一个简化 coding-agent trajectory 改写成 SFT 样本。
- 写 3 条 generation 参数可能影响 coding-agent eval 的例子。

### B 线：Prompt Construction 与 Task Lifecycle

材料：

- `server/internal/daemon/prompt.go`。
- `server/internal/service/task.go`。
- `server/internal/handler/task_lifecycle.go`。

要搞清楚：

- assignment task、comment-triggered task、chat task、autopilot task 的 prompt 差异。
- task 从 queued 到 started/completed/failed/cancelled 的生命周期。
- task run 里哪些字段应该进入 episodic memory。
- failed/cancelled task 是否也有记忆价值。

建议动作：

- 画 task lifecycle 图，先画状态，再补事件触发。
- 把 prompt construction 中引入的上下文分成：用户意图、repo 状态、历史状态、工具/环境约束。
- 写一段说明：为什么 task run 是 episodic memory。

## 今日产出

- 一个最小 causal LM loss 例子。
- 一个 coding-agent trajectory -> SFT 样本例子。
- 一个 Multica task lifecycle 图。
- 一段 prompt construction 总结。

## 验收标准

- 能说清楚 SFT 样本到底在训练模型预测哪一段输出。
- 能解释 generation 参数为什么会影响 eval 结论。
- 能把 Multica task run 映射成 episodic memory schema 的候选字段。

## A 线进度


## B 线进度


## 实际产出

- 

## 问题与卡点

- 

## 明日调整

- 
