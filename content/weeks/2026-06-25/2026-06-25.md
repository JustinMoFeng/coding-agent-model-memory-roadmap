# 2026-06-25

## 今日目标

- A 线：建立模型后训练主线，理解 `token -> causal LM loss -> SFT data` 这条最小链路。
- B 线：完成 Multica 第一轮产品与架构定位，知道它的核心对象是什么。
- 产出：一份主线笔记初稿，以及一张 Multica 核心对象表。

## 学习设计

### A 线：模型后训练主线

材料：

- [大模型 / 后训练学习主线](../../archive/07-model-learning-path.md) 的 M1-M2。
- Stanford CS336 课程主页，先看课程结构，重点定位 tokenizer、language modeling、training、post-training 在整门课中的位置。
- Hugging Face Transformers causal language modeling 文档，只看输入、label、loss 的基本关系：
  - https://huggingface.co/docs/transformers/en/tasks/language_modeling
  - https://huggingface.co/docs/transformers/en/model_doc/gpt2#transformers.GPT2LMHeadModel.forward

要搞清楚：

- Pretraining、SFT、DPO、RLVR/GRPO 分别在改变什么。
- 为什么 language model 的基础训练目标是 next-token prediction。
- 为什么 coding-agent trajectory 可以被整理成 SFT 数据。
- 模型后训练和你现在做的数据合成之间是什么关系。

建议动作：

- 不要完整刷课，只做"地图校准"。
- 写 4 个一句话定义：pretraining、SFT、DPO、RLVR。
- 写 1 段话解释：真实 coding-agent 失败如何转成数据、reward、eval 和 post-training 实验。

### B 线：Multica 核心对象

材料：

- Multica `README.zh-CN.md`。
- Multica `README.md`。
- Multica `CLI_AND_DAEMON.md` 的开头和架构相关部分。
- [Multica Memory 调研指南](../../topics/multica-memory-reading-guide.md) 的"第一层：产品与架构理解"。

要搞清楚：

- Multica 是 managed agents platform，不是普通聊天应用。
- 核心对象包括 workspace、agent、issue、comment、task、runtime、daemon、skill、squad、autopilot。
- issue、comment、task run 为什么天然像 episodic memory。
- skill 为什么可能接近 procedural memory。

建议动作：

- 先画对象关系，不急着读细节代码。
- 每个对象只写三列：它是什么、谁创建它、它会不会成为长期记忆。
- 标记你不确定的问题，后面读代码时验证。

## 今日产出

- `notes/2026-06-25-cs336-lecture-01.md` 初稿。
- Multica 核心对象表。
- 3 个待验证问题。

## 验收标准

- 能用自己的话解释 `token -> loss -> SFT data`。
- 能说清楚 Multica 里 issue、comment、task、skill 分别承担什么角色。
- 能写出至少 3 个"这个对象是否应该进入长期 memory"的判断。

## A 线进度

### CS336 Lecture 1: Overview & Tokenization ✅

- 笔记：[CS336 Lecture 1 — 核心原则](../../notes/2026-06-25-cs336-lecture-01.md)
- 记录了图里的 high-level principle：模型设计是在 **Expressivity / Stability / Efficiency** 三者之间做平衡。
- 记录待读材料：**How to Scale Your Model**，用于之后补 LLM 系统 scaling 的高层概念。
- 记录待读视频：Andrej Karpathy 的 **Let's build the GPT Tokenizer**，用于之后补 tokenizer / BPE。
- 梳理课程大纲：Basics / Systems / Scaling Laws / Data / Alignment，对应 5 个 assignments。
- 开始 Tokenizer 细节：记录第一块 observations，包括空格并入 token、句首/句中 token 不同、数字按几位切分。
- 记录讲义链接和 compression ratio：bytes/token 越高，序列越短；但增大 vocab 会带来 sparsity。
- 梳理四种 tokenizer：character / byte / word / BPE，并用同一个例子比较 vocab size、compression ratio 和未知词处理。
- 标记明天实践：BPE tokenizer implementation，包括高效 `encode()`、special tokens、pre-tokenization 和速度优化。
- 补充 Hugging Face Causal LM 阅读材料：任务页和 GPT-2 forward 文档，下一步读 `input_ids -> labels -> loss`。
- 保存 `input_ids / labels` 错位关系图到 `content/assets/cs336/input-labels-shift.png`，并写入 Causal LM 笔记。
- 整理 4 个一句话定义：pretraining、SFT、DPO、RLVR/GRPO，并校正 DPO 与 PPO/RLVR 的边界。
- 记录 coding-agent failure 转 post-training 的流程：BoN 判定能力边界，成功轨迹做 SFT，强模型 hint 后 mask 做 SFT，或改造成 RLVR/GRPO 题目。
- 写完 `token -> causal LM loss -> SFT data` 最小链路总结：trajectory 可以被序列化成 token，SFT 仍然是 next-token prediction，只是数据分布和 loss mask 变了。

### A 线剩余事项

- [x] CS336 课程结构和 tokenizer 第一节
- [x] Hugging Face Causal LM 文档：看 `input_ids -> labels -> loss` 如何对应 next-token prediction
- [x] 写 4 个一句话定义：pretraining、SFT、DPO、RLVR/GRPO
- [x] 写 1 段话：真实 coding-agent 失败如何转成数据、reward、eval 和 post-training 实验
- [x] 如果时间够：把 `token -> causal LM loss -> SFT data` 串成一段自己的解释

## B 线进度

延期到明天。今天先收住 A 线，不继续推进 Multica。

## 实际产出

- [CS336 Lecture 1 笔记](../../notes/2026-06-25-cs336-lecture-01.md) — 记录 Expressivity / Stability / Efficiency 原则、待读材料、tokenization 推荐视频、课程大纲、tokenizer observations、compression ratio、四种 tokenizer 对比、Causal LM input/label 关系图、4 个后训练定义、coding-agent failure 转训练数据/RL 题目的流程和 `token -> loss -> SFT data` 最小链路

## 问题与卡点

- B 线今天精力不足，顺延到明天做 Multica 核心对象 / memory-like state 调研。

## 明日调整

- A 线：做 tokenizer 实践，从最小 BPE 开始，实现/理解 `train_bpe`、`BPETokenizer.encode/decode`，再看 special tokens、pre-tokenization 和速度优化。
- B 线：开始 Multica 核心对象 / memory-like state 调研。
